<Window x:Class="PrompterMaxF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Prompter;assembly=Prompter"
        xmlns:local="clr-namespace:PrompterMaxF"
        mc:Ignorable="d"
        d:DataContext="{x:Static vm:AppMain.mainDesignVm}"
        Title="MainWindow" Height="150" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="Create Prompts" Command="{Binding ExtractorShow}"/>
        <Button Grid.Row="1" Content="Prompter" Command="{Binding ExtractorClose}"/>

    </Grid>
</Window>
